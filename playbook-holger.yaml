---
- name: your playbook name
  hosts: all
  tasks:
    - name: Create a file called '/tmp/testfile.txt' with the content 'latest version'.
      copy:
        content: 'latest version'
        dest: /tmp/testfile.txt
